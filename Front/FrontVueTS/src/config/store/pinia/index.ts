import { createPinia, Pinia } from 'pinia'

const store: Pinia = createPinia() /* TODO create interface with all the wanted behaviour
like create value, read value,  set value etc... */

export default store